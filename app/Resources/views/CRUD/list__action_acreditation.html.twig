{% if admin.isGranted('VIEW', object) and admin.hasRoute('show') %}
<a class="btn btn-sm btn-default" href="{{ admin.generateObjectUrl('acreditation', object) }}"><i class="glyphicon glyphicon-save"></i> Descargar Acreditaciones</a>
{% endif %}