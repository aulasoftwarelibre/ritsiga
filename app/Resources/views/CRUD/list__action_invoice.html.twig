{% if admin.isGranted('VIEW', object) and admin.hasRoute('show') %}
<a class="btn btn-sm btn-default" href="{{ admin.generateObjectUrl('invoice', object) }}"><i class="glyphicon glyphicon-save"></i> Descargar Facturas</a>
{% endif %}